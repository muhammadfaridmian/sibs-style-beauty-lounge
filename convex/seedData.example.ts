// Example seed data env variables (do NOT commit your real credentials).
// Copy values you need into convex/.env.local or your project root .env.local
// and never commit that file to Git.

/*
DEFAULT_ADMIN_FULLNAME="Sibs Style Admin"
DEFAULT_ADMIN_EMAIL="admin@example.com"
DEFAULT_ADMIN_PHONE="000-000-0000"
DEFAULT_ADMIN_PASSWORD="<a-strong-password-you-set-locally>"

Example .env.local location:
  convex/.env.local    <-- preferred (ignored by git)
  or
  .env.local           <-- project root (ignored by git)

This example file is intentionally safe and contains no real secrets.
*/

export const example = true;
